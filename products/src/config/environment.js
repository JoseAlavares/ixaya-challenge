const config = {
    microservicesNameSpaces: {
        product: {
            name: 'responder_product',
            key: 'product',
            types: {
                getProducts: 'get-products'
            }
        }
    }
}

module.exports = { config }